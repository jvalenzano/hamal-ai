# Phase 4: Prototyping ⚙️

> **"Build the skateboard, not the wheel."**

## Goal
To define the absolute minimum feature set (MVP) required to solve the core problem.

## Artifacts
*   `[idea]-spec.md`: Output of `/prototype-spec`.
*   `implementation_plan.md`: The build plan.

## Instructions
1.  Run `@agent run /prototype-spec`.
2.  Use the output to guide the coding phase.
